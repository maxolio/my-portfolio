# 🌐 Personal Developer Portfolio — Сайт-портфолио фронтенд-разработчика

Минималистичный сайт-портфолио, созданный для подведения итогов первого года обучения фронтенд-разработке. Проект призван наглядно продемонстрировать навыки верстки, работы с чистым JavaScript (без использования фреймворков), архитектурой БЭМ и проектирования пользовательских интерфейсов.

---

## ✨ Ключевые особенности
* **Переключение тем (Dark / Light)** — поддержка темной и светлой темы с сохранением выбора пользователя в `localStorage` и поддержкой системных настроек ОС (`prefers-color-scheme`).
* **Мультиязычность (RU / EN)** — динамическая смена языка интерфейса через JS-словарь "на лету" без перезагрузки страницы.
* **Чистый стек без фреймворков** — демонстрация фундаментальных знаний HTML, SCSS и Vanilla JS.
* **Адаптивный & Доступный дизайн** — корректное отображение на всех типах устройств с соблюдением стандартов доступности (Accessibility).

---

## 🛠️ Стек технологий и инструменты

* **HTML5** — семантическая и доступная разметка (использование тегов `<header>`, `<main>`, `<section>`, `<footer>` и атрибутов `data-*`).
* **CSS3 / SCSS** — модульная стилизация, реализация темной/светлой темы через CSS Custom Properties (переменные), построение сеток (Flexbox, Grid).
* **BEM (БЭМ)** — методология именования классов для создания поддерживаемой структуры стилей.
* **JavaScript (ES6+)** — логика переключения тем и языков, манипуляции с DOM, работа с Web Storage API (`localStorage`).
* **NPM & Tooling** — сборка и слежка за SCSS (`sass`), локальный сервер с автоперезагрузкой (`live-server`).
* **Git & GitHub** — контроль версий, история разработки и публикация проекта.
* **Repomix** — упаковывание контекста проекта для анализа с помощью ИИ-ассистентов.

---

## 📝 Дневник разработки (Changelog)

<details>
<summary>
<b>Этап 1: Инициализация проекта, настройка окружения и архитектуры</b>
</summary>

На данном этапе развернута инфраструктура проекта, настроена автоматизация разработки и сформирован базовый фундамент стилей.

* **Инициализация и сборка**:
    * Проект инициализирован через `npm`. Настроены скрипты для автоматической компиляции SCSS в CSS и запуска локального сервера `live-server`.
    * Установлена и настроена утилита **Repomix** для экспорта исходного кода. Создан файл `.repomixignore` для отсечения графики и скомпилированных файлов из контекста ИИ.

* **Контроль версий**:
    * Создан и настроен файл `.gitignore` (исключены `node_modules/`, `.idea/`, карты исходников `*.map` и системные файлы).
    * Локальный репозиторий связан с удаленным на GitHub (`maxolio/my-portfolio`).

* **Архитектура стилей и разметка**:
    * Подключены современный сброс стилей (CSS Reset) и базовые типографические настройки.
    * Заложена архитектура CSS-переменных (`:root` и `[data-theme="dark"]`) для реализации темной и светлой темы.
    * Подготовлен семантический каркас `index.html`.

</details>

<details>
<summary>
<b>Этап 2: Реализация темной/светлой темы и мультиязычности (RU/EN)</b>
</summary>

На данном этапе реализована интерактивная логика переключения тем и языков интерфейса на чистом JavaScript с использованием ООП-подхода (классы) и Web Storage API (`localStorage`).

* **Архитектура скриптов (OOP)**:
    * Созданы изолированные ES6-классы `ThemeToggle` и `LanguageToggle` по единому архитектурному шаблону (управление селекторами, состояниями, инициализация и привязка событий).
* **Интерактивность и Доступность (a11y)**:
    * Настроено переключение тем (`dark` / `light`) с изменением CSS-переменных и синхронизацией состояния `aria-checked` для скринридеров.
    * Реализована система мультиязычности через словарь (`ru` / `en`) и атрибуты `data-i18n` для динамической смены текста "на лету" без перезагрузки страницы.
* **Сохранение пользовательских данных**:
    * Выбор темы и языка автоматически сохраняется в `localStorage` браузера и восстанавливается при повторном посещении сайта.

</details>

<details>
<summary>
<b>Этап 3: Верстка главного экрана (Hero), блока автора и социальных ссылок</b>
</summary>

На данном этапе сверстана главная секция сайта (`hero`), включающая в себя информацию об авторе, текстовое приветствие и интерактивные кнопки социальных сетей.

* **Семантика и Архитектура БЭМ**:
    * Сверстан независимый компонент автора (`author`) с аватаркой, именем и профессией.
    * Реализована секция `hero__bio` с гибкой текстовой структурой, готовой к мультиязычности через атрибуты `data-i18n`.
    * Создан переиспользуемый компонент кнопок (`button`) с интегрированными SVG-иконками внешних ссылок.
* **Доступность и Безопасность (a11y & Security)**:
    * В секцию добавлен скрытый заголовок `h1` (`visually-hidden`) для корректной структуры документа и SEO.
    * Все внешние ссылки с `target="_blank"` защищены атрибутами `rel="noopener noreferrer"`, а декоративные SVG-иконки скрыты от скринридеров через `aria-hidden="true"`.
* **Модульная стилизация (SCSS)**:
    * Созданы отдельные SCSS-модули для каждого блока (`_hero.scss`, `_author.scss`, `_button.scss`, `_soc1a1s.scss`).
    * Настроены адаптивные отступы и размеры шрифтов с помощью встроенных функций `fluid()` и `rem()`.

</details>

<details>
<summary>
<b>Этап 4: Верстка компонента вкладок (Tabs) и карточек проектов (Промежуточная версия без JS)</b>
</summary>

На данном этапе развернута статическая разметка системы вкладок для будущей фильтрации работ и разработаны карточки проектов с детальной информацией, тегами и ссылками.

* **Разметка вкладок (Tabs Layout)**:
    * Сверстан адаптивный интерфейс категорий проектов с полной поддержкой ARIA-атрибутов (`role="tablist"`, `role="tab"`, `aria-selected`, `aria-controls`), подготовленный к оживлению через JavaScript.
    * Настроена ограниченная по высоте область `.tabs__list` с кастомной вертикальной прокруткой для удобного просмотра списков проектов.
* **Карточки проектов (Project Cards)**:
    * Разработан компонент `project-card` с изображениями проектов, описаниями и внешними ссылками на код и демо.
    * Добавлена цветовая маркировка для стека технологий (`HTML`, `SCSS`, `BEM`, `JS`).
* **Модульная стилизация (SCSS)**:
    * Стилизованы отдельные модули `_tabs.scss`, `_project-card.scss`, `_tags.scss`.

</details>

<details>
<summary>
<b>Этап 5: Секция навыков, тема "Винтажный винил" и фиксы компонентов</b>
</summary>

На данном этапе добавлена секция навыков и карты развития (`skills`), кастомизирована палитра светлой темы и исправлены мелкие баги стилизации.

* **Секция навыков и карты развития (Skills & Roadmap)**:
    * Развернут компонент `skills` с разделением навыков на три категории: освоенные технологии, текущий процесс и планы на будущее (Roadmap).
    * Добавлена мультиязычность для всех названий статусов и заголовков через атрибуты `data-i18n`.
* **Кастомизация тем (Theming)**:
    * Добавлена авторская светлая тема **"Винтажный винил"** (`#c8c6c1`) с мягкими графитовыми и терракотовыми акцентами.
* **Исправление стилей и интерактивности (Bug Fixes & UI)**:
    * Скорректированы ховер-эффекты для описаний карточек проектов (`project-card__description`).
    * Настроены интерактивные почтовые ссылки с использованием протокола `mailto:`.

</details>